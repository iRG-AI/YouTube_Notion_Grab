# Skill: Security and Safety Guidelines for Google APIs
name: security-safety-guidelines
description: 구글 API 보안 정책 준수 및 예외 상황(Suspended 등) 발생 시 사용자 계정 연쇄 정지 예방을 위한 안전한 코딩 가이드라인

---

## 📌 핵심 개요
본 지침은 Google API, Notion API 등 외부 연동 시스템의 자동화 코드를 작성할 때 **사용자 계정 보호 및 Abuse 차단**을 최우선으로 확보하기 위한 영구적인 개발 지침(Skill)입니다. 

기능 구현이 정상적으로 완료되더라도, 예외 상황에 대한 안전장치가 없다면 계정이 영구 정지될 위험이 있습니다. 개발 시 반드시 본 규칙을 1순위로 고려해야 합니다.

---

## 🚨 1. 치명적 보안 에러 발생 시 즉각 종료 (Kill-switch)
API 요청의 실패 원인이 **권한 차단 또는 차단 조치(Suspended)**와 관련된 경우, 시스템은 절대 재시도를 시도해서는 안 됩니다.

* **감지 대상 에러**:
  * HTTP `403 Forbidden` (특히 구글 API의 `CONSUMER_SUSPENDED`, `suspended` 상태)
  * HTTP `401 Unauthorized` (인증 정보 유실 또는 차단)
  * HTTP `400 Bad Request` (구조적 통신 오류)
* **대처 표준 원칙**:
  1. 감지 즉시 루프 및 키 로테이션을 **즉각 중단(Break/Exit)**합니다.
  2. 에러 로그에 치명적 위험이 감지되었음을 명확히 기록합니다.
  3. 가능하다면 텔레그램/이메일 등 안전한 통로로 사용자에게 **긴급 보안 경고 알림**을 1회 전송합니다.
  4. 추가적인 API 호출을 완벽하게 막기 위해 즉시 **프로세스를 강제 종료(`process.exit(1)` 또는 `sys.exit(1)`)**합니다.

---

## ⚠️ 2. 계정 정지(Suspension) 유발 패턴 금지
구글 보안 봇은 특정 패턴의 자동화 코드를 "계정 도용 및 불법 어뷰징 행위"로 간주하여 계정을 잠급니다. 아래의 패턴은 절대 구현하지 마십시오.

* **금지 패턴 1: 무작정 빠른 재시도 (Fast Retry Loops)**
  * 에러가 났을 때 대기 시간 없이 즉시 다음 루프로 넘어가거나, 수 초 미만의 아주 짧은 시간 간격으로 API 요청을 연속 송신하는 행위.
* **금지 패턴 2: 무한 키 로테이션 (Infinite Rotation)**
  * 키가 차단되었음에도 불구하고 다른 키로 계속 로테이션하며 전체 에러 요청 횟수를 늘리는 행위.
* **금지 패턴 3: 부적절한 사용자 에이전트(User-Agent) 및 비정상 접근 패턴**
  * 새로운 로컬 실행 환경에서 보안 승인 절차 없이 갑자기 대규모 쓰기 API 요청을 쏟아내는 행위.

---

## ⏳ 3. 안전한 백오프 및 호출 빈도 제어 (Rate Limiting)
일시적인 API 에러(Rate Limit 429, 임시 서버 불안정 500/503 등)를 복구할 때는 **반드시 충분한 간격**을 두어야 합니다.

* **지수 백오프 (Exponential Backoff)** 필수 적용:
  * 에러 발생 시 다음 재시도까지의 대기 시간은 기하급수적으로 늘어나야 합니다.
  * 예: `WaitTime = Min(MaxWaitTime, BaseWaitTime * (2 ^ Attempt))`
* **최대 재시도 한도 (Max Attempts)** 제한:
  * 일시적 오류라 하더라도 최대 재시도 횟수(예: 3~5회)를 명확히 제한하고, 한도 초과 시 작동을 안전하게 중지해야 합니다.

---

## 🔒 4. 테스트 격리 환경 구축 권고
운영 및 개발을 진행할 때 사용자의 실제 개인 계정을 직접 API에 연결하면 정지 시 막대한 피해를 줍니다.

* **계정 격리(Isolation)**:
  * 실제 주소록, 메일, 캘린더가 묶인 개인 구글 계정 대신, 자동화 테스트만을 위해 생성한 전용 구글 계정(예: `dev.xxx@gmail.com`)을 생성하도록 유도합니다.
  * 해당 격리 계정 내에서만 GCP 프로젝트 및 API Credentials를 생성하고 연결하여 최악의 사태(계정 정지) 시 피해 범위가 전파되는 것을 원천 차단합니다.
